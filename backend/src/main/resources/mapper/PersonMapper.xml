<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aipm.backend.mapper.PersonMapper">
    <resultMap id="BaseResultMap" type="com.aipm.backend.entity.Person">
        <id column="id" property="id" />
        <result column="employee_no" property="employeeNo" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="real_name" property="realName" />
        <result column="phone" property="phone" />
        <result column="role" property="role" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM users ORDER BY created_at DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (employee_no, username, password, real_name, phone, role)
        VALUES (#{employeeNo}, #{username}, #{password}, #{realName}, #{phone}, #{role})
    </insert>

    <update id="update">
        UPDATE users
        SET employee_no = #{employeeNo}, username = #{username}, real_name = #{realName}, phone = #{phone}, role = #{role}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM users WHERE id = #{id}
    </delete>
</mapper>
